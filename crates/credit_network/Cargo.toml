[package]
name = "credit_network"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
spectral_hypergraph = { path = "../spectral_hypergraph", features = ["serde-support"] }
ontology_engine = { path = "../ontology_engine" }
entity = { path = "../entity" }
uuid = { version = "1", features = ["v4", "serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
