[package]
name = "credit_risk"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
neural_hmm = { path = "../credit_risk_hmm_engine" }
burn = { version = "0.13", default-features = false, features = ["ndarray", "std"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
